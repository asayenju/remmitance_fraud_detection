# remmitance_fraud_detection

## Docker Usage
- Docker is used to run multiple servers or applications at the same time
- Docker helps in running and downloading dependencies for each program consistently in every local machine

## How to use Docker:
- Download Docker Desktop
- To run docker:
```bash
docker-compose up --build
```
- To stop the services:
```bash
docker-compose down
```